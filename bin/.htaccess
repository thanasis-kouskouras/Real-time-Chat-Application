# Deny all direct web access to bin/.

# Scripts here are CLI entry points (chat-server.php, process-email-queue.php) and must never be reachable over HTTP.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
Order Allow,Deny
Deny from all
</IfModule>
